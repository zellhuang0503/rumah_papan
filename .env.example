# Sanity CMS Configuration (Public - safe to expose)
VITE_SANITY_PROJECT_ID=your_project_id
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-03-24

# Sanity Write Token (PRIVATE - NEVER commit this!)
# Only used for migration scripts, not exposed to frontend
SANITY_API_WRITE_TOKEN=your_write_token_here
