# CMS 功能更新與操作指南 v3.0
**文件編號**：DOC-20260116-v3
**日期**：2026-01-16
**適用對象**：網站管理員、內容編輯人員 (黃老闆)

---

## 📋 本次更新總覽

今晚的開發重點在於提升首頁與全站資訊的**後台可編輯性**，並優化了部分視覺細節。

### 主要更新項目
1.  **首頁輪播圖 (Intro Slides)**：全面整合至 Sanity 後台，支援圖文自由編輯。
2.  **頁尾資訊 (Footer)**：開放後台修改網站標題、聯絡電話、地址等資訊。
3.  **創辦人照片**：更新聯絡頁面的創辦人照片素材。
4.  **自動化資料遷移**：建立自動化腳本，將既有內容無痛搬移至資料庫。
5.  **視覺優化**：調整首頁輪播圖的黑色遮罩漸層，提升通透感。

---

## 🛠️ 功能操作說明

### 1. 首頁輪播圖管理 (Intro Slides)
現在您可以隨時更換新村大門的輪播照片與文案。

*   **操作路徑**：進入 CMS 後台 -> 點選左側 **「首頁 (Home)」**。
*   **功能區塊**：找到 **「新村大門輪播圖 (Intro Slides)」** 欄位。
*   **操作方式**：
    *   **新增**：點擊「Add Item」新增一張投影片。
    *   **排序**：按住左側六個點的圖示，拖拉即可調整順序。
    *   **刪除**：點選項目右側選單可刪除。
*   **防呆機制**：如果後台不小心把所有圖片都刪光了，前端網站會自動顯示「系統預設」的那組照片 (雞公碗等)，確保版面永遠有東西看。
*   **視覺優化**：圖片下方的黑色漸層已從 80% 調整為 **40%**，讓畫面更清爽。

### 2. 頁尾資訊管理 (Site Footer)
全站頁尾的聯絡資訊與標題現在都統一管理。

*   **操作路徑**：進入 CMS 後台 -> 點選左側 **「網站設定 (Site Settings)」**。
*   **可編輯欄位**：
    *   **網站主標題 & 副標題**
    *   **描述 (Description)**
    *   **聯絡資訊**：包含 服務區域、電話、Email、地址。
*   **即時更新**：修改後發布，全站每一頁的頁尾都會立刻同步更新。

---

## 👨‍💻 技術與維護筆記 (Developer Notes)

### 1. 資料遷移腳本 (Migration Script)
為了避免手動上傳大量圖片，本次建立了自動化搬運腳本。
*   **腳本位置**：`rumah_papan_studio/scripts/migrateSlides.js`
*   **用途**：讀取本地 `src/assets/images` 圖片並批量上傳至 Sanity。
*   **執行指令**：
    ```bash
    npx sanity exec scripts/migrateSlides.js --with-user-token
    ```
    *(需先執行 `npx sanity login` 確保權限)*

### 2. 資料結構更新 (Schema Changes)
*   **schemas/home.ts**：新增 `introSlides` 陣列。
*   **schemas/siteSettings.ts**：新增 `subtitle`, `phone`, `address`, `serviceRegion` 等欄位。
*   **schemas/village.ts**：修復了後台列表預覽 (Preview) 顯示 JSON 原始碼的問題，現在能正確顯示中文標題。

### 3. Git 版本控制
*   **Frontend Repo** (`rumah_papan`)：已推送至 `feature/cms-sanity` 分支。
*   **Studio Repo** (`rumah_papan_studio`)：已推送至 `main` 分支。
*   兩端代碼均已同步至 GitHub，版號一致。

---

*文件製作：AI 代理人 (Claude 4.5 Opus / Gemini 3.0 Pro)*
